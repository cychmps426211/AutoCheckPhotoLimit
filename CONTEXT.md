# AutoCheckPhotoLimit Context

自動查詢與監控後台機台底片存量的 Line 機器人系統。

## Language

**維修師 (Technician)**:
負責機台維護與底片耗材補充的現場人員，後台系統中以 `uno` (編號) 識別。
_Avoid_: 員工, 管理員, 使用者

**機台 (Machine)**:
擺放於各站點的相片列印設備，具有專屬代號與安裝位置。
_Avoid_: 設備, 機器, 裝置

**底片存量 (Film Stock)**:
機台內部目前剩餘的底片/相紙張數或狀態級別。
_Avoid_: 庫存, 紙張, 耗材

**底片狀態類別 (Stock Status Category)**:
後台系統篩選底片狀態的固定分類碼（0 = 全部，1 = 充足，2 = 接近底限）。
_Avoid_: 庫存等級, 狀態碼

**剩餘張數 (Remaining Sheets)**:
特定機台即時回報的底片剩餘精確張數。
_Avoid_: 剩餘數量, 庫存量

**驗證碼 (Captcha)**:
後台登入頁面由 `GetVCode.php` 生成的 4 位數字驗證碼。
_Avoid_: 認證碼, 檢查碼

**預設維修師 (Default Technician)**:
未於指令中指定維修師時，系統預設查詢的維修師（編號 `91`）；執行預設查詢時會一併納入設定檔中定義之協同機台。
_Avoid_: 預設使用者, 預設人員

**預設查詢 (Default Query)**:
未於指令中指定狀態與張數門檻時（發送「底片」或「檢查底片」），系統預設查詢全部底片狀態（`s=0`）且剩餘張數小於等於 20 張（`threshold=20`）之機台，並自動合併協同機台。
_Avoid_: 一般查詢, 快速查詢

**異常機台 (Abnormal Machine)**:
後台系統中因硬體故障、撤除或測試用途而回報異常底片數據之特定機台（例如 `ABC158-ND 高雄職訓中心`），系統於所有查詢結果中自動予以過濾排除，防止誤報。
_Avoid_: 故障機台, 壞掉機台, 黑名單機台

**協同機台 (Collaborative Machine)**:
非預設維修師名下，但由該維修師負責巡檢或補充底片之跨維修師指定機台（例如維修師 19 位於高雄之指定機台清單）。
_Avoid_: 外包機台, 支援機台, 跨區機台

**剩餘張數門檻 (Threshold)**:
用於過濾警報機台的剩餘底片張數上限值，僅列出小於等於該門檻的機台。
_Avoid_: 警戒線, 警報值

**回覆令牌 (Reply Token)**:
Line 平台隨 Webhook 事件傳入的單次訊息回覆憑證，支援無限次免費被動回覆。
_Avoid_: 訊息憑證, 回覆密鑰

**緊急排序 (Urgency Sorting)**:
機台清單一律依「剩餘張數」由小到大（升冪）排列，最缺紙的機台優先置頂。
_Avoid_: 預設排序, 名稱排序

**熔斷保護 (Circuit Breaker)**:
當後台登入連續失敗達門檻（3 次）時，系統自動中斷重試並暫停請求，防止帳號鎖定或雪崩效應的自我保護機制。
_Avoid_: 斷路器, 停止服務, 當機保護

**心跳保活 (Keep-Alive)**:
由外部定時排程服務定期發送請求至 `/health` 端點，以維持雲端主機常駐不休眠並探測後台會話活躍度的機制。
_Avoid_: 保溫, 喚醒程式, 存活檢查

**值班底片殘量查詢 (On-Duty Stock Query)**:
現場維修師輪值值班時跨越個人名下機台限制，依指定責任區域（預設南區，`area=46`）查詢全部狀態（`s=0`）且剩餘張數小於等於門檻（預設 20 張）之全區機台存量，自動過濾異常機台、落實緊急排序，並呈現各機台之後台責任維修師。
_Avoid_: 區域查詢, 南區查詢, 值班查全部

**責任區域 (Duty Area)**:
後台系統所定義之機台分區代碼（例如 46 代表南區），用於值班時界定跨維修師巡檢範圍。
_Avoid_: 巡檢區, 管區, 責任範圍

**維護行程 (Maintenance Schedule)**:
現場維修師於各機台巡檢或補充耗材之每日行程紀錄（由後台 `UserMonth.php` 依維修師 `uno` 與指定月份 `m1` 呈現），記錄機台編號、名稱與到達維護時間。
_Avoid_: 班表, 簽到, 路線

**定時推播 (Scheduled Push)**:
系統於指定工作日時間點，由外部排程觸發自動執行南區值班底片殘量查詢（`area=46, s=0`），並於有機台低於門檻時透過 Line Broadcast API 主動向維修師發送值班警報之機制。若機台存量皆充足則靜默不發送，嚴格控管免費推播額度。
_Avoid_: 自動發話, 定時通知, 廣播推播

